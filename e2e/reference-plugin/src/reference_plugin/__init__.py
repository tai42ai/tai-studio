"""Reference Studio plugin used by the tai-studio end-to-end suite.

Importing :mod:`reference_plugin.tools` runs the ``@tai_app.tools.tool``
registration decorators. The Studio front-end half of the plugin (a page and a
tool panel, built from ``studio-src/``) is compiled into the ``studio/``
directory and served by the skeleton's plugin router.
"""
