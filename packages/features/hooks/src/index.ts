/**
 * @tai42/feature-hooks — the hooks surface. Exposes one shell page: a topic
 * filter, a table of registered lifecycle hooks with per-row delete, and a
 * register form. Imports only @tai42/studio-sdk, @tai42/api-client (via the SDK's
 * typed client), and TanStack Query.
 */
export { HooksPage } from './HooksPage';
