// @tai42/feature-manifest — the manifest surface: inspect the skeleton's loaded
// manifest, the MCP server status + config, and the derived sub-MCP servers.
export { ManifestPage } from './ManifestPage';
