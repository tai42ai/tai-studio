// @tai42/feature-manifest — the manifest surfaces: the loaded-manifest config
// artifact view (Administration) and the Served Endpoints page (the promoted
// sub-MCP servers, a Connections surface).
export { ManifestPage } from './ManifestPage';
export { ServedEndpointsPage } from './ServedEndpointsPage';
