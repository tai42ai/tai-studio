/**
 * @tai42/feature-marketplace — the marketplace surface. Exposes one shell page:
 * listing-level browse/search over the curated plugin registry, a plugin detail
 * view with versions and advisories, and install / update / uninstall actions
 * driven through the skeleton's marketplace routes. Imports only
 * @tai42/studio-sdk, @tai42/api-client, and TanStack Query.
 */
export { MarketplacePage } from './MarketplacePage';
