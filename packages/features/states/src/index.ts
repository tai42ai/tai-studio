/**
 * @tai42/feature-states — the States surface. A master/detail page over the
 * subject-keyed state store: a management table of declared states (with a
 * declare-state dialog and an upload door), a per-state detail with Declaration,
 * Modules, Records and Consumers tabs, and a per-subject record page (document
 * editor, fold, and the write audit trail). Imports only @tai42/studio-sdk,
 * @tai42/api-client, and TanStack Query.
 */
export { StatesPage } from './StatesPage';
